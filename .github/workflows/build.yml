name: build

on:
  push: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          submodules: true
      - name: checkout public
        uses: actions/checkout@v2
        with:
          path: public
          fetch-depth: 0
          ref: master
